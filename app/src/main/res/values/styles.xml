<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.



    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->

        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowSharedElementEnterTransition">
            @transition/change_people_or_event_input
        </item>

    </style>

    <style name="dialog_animation_fade">
        <item name="android:windowEnterAnimation">@transition/fade_in_dialog</item>
        <item name="android:windowExitAnimation">@transition/fade_out_dialog</item>
    </style>

    <color name="theme">#ff4f6069</color>

    <!-- Event Divider style -->
    <style name="EventDivider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">#CFD8DC</item>
    </style>

    <style name="ActionBarDivider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">3dp</item>
        <item name="android:background">#5ccfd8dc</item>
    </style>

    <!-- Event Title Display style -->
    <style name="EventTitleDivider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">4dp</item>
        <item name="android:background">#ff394246</item>
    </style>

    <!-- Event Vertical Divider style -->
    <style name="EventVerticalDivider">
        <item name="android:layout_width">2dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">#CFD8DC</item>
    </style>

    <!-- Event Top Hint TextView style -->
    <style name="TopHintTextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#33B5E5</item>
        <item name="android:textSize">13sp</item>
        <item name="android:layout_marginLeft">10dp</item>
    </style>

    <!-- Event EditText style -->
    <style name="EventEditText">
        <item name="android:textSize">16sp</item>
        <item name="android:background">@null</item>
        <item name="android:layout_marginLeft">55dp</item>
        <item name="android:layout_marginStart">55dp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

</resources>